L = ['yellow', 'red', 'blue', 'green', 'black'] # Create

L2 = ['Black', 'Brown']

L [0] # Acces

L[2:] # Slicing

len(L) # Length

sorted(L) # Sorting

L.index('blue') # En qué posición está blue

L.append("pink") # Agrega al final

L.insert(0, "white") # Agrega en la posición enviada

L.extend(L2) # Suma una lista en la otra

L.remove("white") # Borra el valor enviado

del L[0] # Borra el valor por index

L.pop() # Borra último item & devuelve su valor

L.reverse()

L.count('red') # Search list and return number of instances found


if 'red' in L: #  Test if an item is in a list
    pass
