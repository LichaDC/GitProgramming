dic = {1: 'Uno', 2:'Dos', 3:'Tres', 4:'Cuatro', 5:'Cinco'} # Create a new dictionary

dic['key'] = 'Value' # Add a new item

del dic['key'] # Delete an item (Key and value)

'key' in dic # Chek if a key exists

(dic.get('key', "something")) # If the key exists return the value, if not returns the second parameter

for key in dic: # Walk by the keys

for key, val in dic: # Walk by the keys and values

dic['key'] # Returns the value

sorted(dic)

print(dic[])
